# Fork update feeds

The sync workflow writes checksum-pinned `latest.json` and `preview.json`
manifests here after publishing verified fork releases. Published binaries
compile these raw GitHub paths as their native stable and preview update feeds.
